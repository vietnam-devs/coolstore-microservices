apiVersion: v1
kind: Service
metadata:
  name: {{ include "shoppingcart-api.fullname" . }}
  labels:
    {{- include "shoppingcart-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.httpNodePort }}
      {{- end }}
      protocol: TCP
      name: http
  selector:
    {{- include "shoppingcart-api.selectorLabels" . | nindent 4 }}
