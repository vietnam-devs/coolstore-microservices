apiVersion: v1
kind: Service
metadata:
  name: {{ include "productcatalog-api.fullname" . }}
  labels:
    {{- include "productcatalog-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.httpNodePort }}
      {{- end }}
      protocol: TCP
      name: http
  selector:
    {{- include "productcatalog-api.selectorLabels" . | nindent 4 }}
