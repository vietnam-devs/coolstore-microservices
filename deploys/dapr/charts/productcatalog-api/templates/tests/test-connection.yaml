apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "productcatalog-api.fullname" . }}-test-connection"
  labels:
    {{- include "productcatalog-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "productcatalog-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
