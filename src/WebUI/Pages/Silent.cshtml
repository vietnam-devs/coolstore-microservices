@page "/silent"
@inject WebUI.Services.JsInterop jsInterop

<NavLink href="/">
  Back to home
</NavLink>

@functions {

  protected override async Task OnInitAsync()
  {
    await jsInterop.Silent();
  }

}
