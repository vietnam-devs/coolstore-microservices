@inherits WebUI.Features.BaseComponent
@page "/callback"

@functions {

  protected override async Task OnInitAsync()
  {
    var user = await JsInteropService.Callback();
    if (user != null)
    {
      await AppState.UpdateUser(user);
      await JsInteropService.Navigate();
    }
  }

}
